[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "vctrs"
version = "0.1.0"
description = "A fast embedded vector database"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "vctrs._vctrs"
