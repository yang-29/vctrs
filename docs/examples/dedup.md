# Deduplication

Find near-duplicate items in a dataset using vector similarity.

=== "Python"

    ```python
    --8<-- "examples/dedup/dedup.py"
    ```

=== "TypeScript"

    ```typescript
    --8<-- "examples/dedup/dedup.ts"
    ```

## Run it

=== "Python"

    ```bash
    pip install vctrs sentence-transformers
    python examples/dedup/dedup.py
    ```

=== "TypeScript"

    ```bash
    npm install @yang-29/vctrs @xenova/transformers
    npx tsx examples/dedup/dedup.ts
    ```
